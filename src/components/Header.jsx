import { Link } from "react-router-dom";

export default function Header() {
  return (
    <header>
      <nav>
        <a href="">HOME</a>
        <a href="">ABOUT</a>
      </nav>

      <nav>
        <a href="">SHOP</a>
        <a href="">CONTACT</a>
      </nav>
    </header>
  );
}
